<!doctype html>
<html>
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Widget</title>
  <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
  <script src="/socket.io/socket.io.js"></script>
  <style>
    html, body { margin:0; padding:0; background: transparent; width:100%; height:100%; }
    #canvas { width: 100%; height: 100%; display:block; }
    .legend { color: white; font-family: Inter, system-ui, Arial; }
    .wrap { position: relative; width:100%; height:100%; }
    #winnerOverlay {
      position: absolute;
      inset: 0;
      display: none;
      align-items: center;
      justify-content: center;
      text-align: center;
      padding: 24px;
      color: #fff;
      font-family: Inter, system-ui, Arial;
      pointer-events: none;
      background: transparent; /* затемнение будет рисоваться на втором canvas */
    }
    #winnerText {
      font-weight: 800;
      text-shadow: 0 2px 6px rgba(0,0,0,.7), 0 0 18px rgba(0,0,0,.6);
      animation: popIn .6s ease both;
      width: 100%;
      line-height: 1.15;
      font-size: clamp(28px, 8vw, 120px);
    }
    @keyframes popIn {
      0% { opacity: 0; transform: scale(.7); }
      60% { opacity: 1; transform: scale(1.08); }
      100% { opacity: 1; transform: scale(1); }
    }
  </style>
</head>
<body>
  <div class="wrap">
    <canvas id="canvas"></canvas>
    <canvas id="shade"></canvas>
    <div id="winnerOverlay"><div id="winnerText"></div></div>
  </div>
  <script src="/static/widget.js" data-token="<%= token %>"></script>
</body>
</html>


